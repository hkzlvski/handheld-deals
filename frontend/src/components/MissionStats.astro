---
interface Props {
  activeDeals: number;
  newVerified: number;
  potentialSavings: number;
}

const { activeDeals, newVerified, potentialSavings } = Astro.props;
---

<div
  class="bg-zinc-900/50 border border-zinc-800 rounded-xl p-6 backdrop-blur-sm"
>
  <div class="flex items-baseline gap-2 mb-4">
    <h2 class="text-sm font-semibold text-zinc-400 uppercase tracking-wide">
      🎯 Active Missions
    </h2>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <!-- Expiring Today -->
    <div
      class="flex flex-col items-center justify-center p-4 bg-zinc-800/50 rounded-lg border border-zinc-700/50"
    >
      <div class="text-3xl font-bold text-deck-orange mb-1 font-mono">
        {activeDeals}
      </div>
      <div class="text-xs text-zinc-400 uppercase tracking-wide text-center">
        ⚡ Expiring Today
      </div>
    </div>

    <!-- New Verified -->
    <div
      class="flex flex-col items-center justify-center p-4 bg-zinc-800/50 rounded-lg border border-zinc-700/50"
    >
      <div class="text-3xl font-bold text-green-400 mb-1 font-mono">
        {newVerified}
      </div>
      <div class="text-xs text-zinc-400 uppercase tracking-wide text-center">
        ✓ New Verified
      </div>
    </div>

    <!-- Potential Savings -->
    <div
      class="flex flex-col items-center justify-center p-4 bg-zinc-800/50 rounded-lg border border-zinc-700/50"
    >
      <div class="text-3xl font-bold text-blue-400 mb-1 font-mono">
        ${potentialSavings}
      </div>
      <div class="text-xs text-zinc-400 uppercase tracking-wide text-center">
        💰 Potential Savings
      </div>
    </div>
  </div>
</div>
