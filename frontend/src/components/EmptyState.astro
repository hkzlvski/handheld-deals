---
interface Props {
  title?: string;
  message?: string;
  linkText?: string;
  linkHref?: string;
}

const {
  title = "No deals found",
  message = "Check back soon for more great deals!",
  linkText = "Browse all deals",
  linkHref = "/browse",
} = Astro.props;
---

<div class="text-center py-12 px-4">
  <div class="text-6xl mb-4">🎮</div>
  <h3 class="text-xl font-bold text-zinc-300 mb-2">{title}</h3>
  <p class="text-zinc-500 mb-6">{message}</p>
  {
    linkHref && (
      <a href={linkHref} class="btn btn-primary inline-flex items-center gap-2">
        {linkText} →
      </a>
    )
  }
</div>
